using System.Collections;
using System.Collections.Generic;
using System.Threading;
using UnityEngine;

public class EnemyType3 : ObjectController
{
    

  
    
    // Start is called before the first frame update
    void Start()
    {
        ScaleModifier = 1.25f;  
        speed = 40f;
    }

    // Update is called once per frame
    
}
